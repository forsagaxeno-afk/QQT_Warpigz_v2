# War Pig — Zewx

**2026-09-23 audited build:** see [AUDIT.md](../audit/baseline-2026-09-23/AUDIT.md) for fixes, installation, offline verification and live-validation limits. The supplied Horde reset/entry fixes are retained.

A curated plugin suite for Diablo IV that runs **War Plans** end-to-end. WarPigs is the master orchestrator: it watches your active War Plan quests and toggles the right sub-plugin for each activity. You play the meta-game — pick the plans you want to run — and WarPigs drives the rest.

## How a full cycle runs

The suite is designed to run end-to-end without you ever opening the War Plan board:

1. **WarPug** picks a War Plan path for you while you're idle in Temis. It uses the in-game `warplan` API to auto-select + confirm a tree, and rerolls (up to 10 attempts) when no NMD-free path exists. You don't have to touch the board.
2. **WarPigs** watches the active `WarPlans_QST_*` quests and enables the right activity plugin (Pit / Helltides / Hordes / Boss Lairs / Undercity) for each step.
3. After every activity (with **Use teleport** on), WarPigs detours through Temis, triggers Alfred for stash / salvage / repair, and waits until he's done before teleporting to the next quest.
4. When the path finishes, WarPigs walks the **Turn-in** task automatically (teleports to Temis → approaches Tyrael → interacts).
5. WarPug picks the next path once cleanup and service gates release. The cycle repeats while a valid plan is available; unresolved activity or companion errors remain visible and hold the next step.

You *can* still build the path manually if you want — WarPigs only activates a plugin when its matching `WarPlans_QST_*` quest is in your log, regardless of whether you or WarPug queued it.

Supported activity types:
- **Helltides** (Tortured Gifts)
- **The Pit**
- **Infernal Hordes**
- **Boss Lairs** (boss runs via Reaper)
- **Kurast Undercity**

> **Nightmare Dungeons are NOT supported.** WarPug always excludes NMD nodes when picking paths, and WarPigs will not activate any plugin for an NMD WarPlan if one is queued manually — no plugin in this suite drives NMDs.

## What's in the suite

| Plugin | Activity | Notes |
|---|---|---|
| **WarPigs-1.0.0** | Master orchestrator | The brain. Watches quests, enables/disables sub-plugins. Always required. |
| **WarPug-1.0.0** | War Plan creator | Companion to WarPigs. When you're in Temis with no active WarPlans quests, auto-selects and confirms a new war plan path using the `warplan` API (Nightmare Dungeon nodes always excluded). Falls back to a configured reroll + confirm pixel click when no valid path exists — coordinates captured by hovering the in-game button and pressing the bound keybind, stored as resolution-independent ratios. |
| **ArkhamAsylum-1.0.6** | The Pit | Pit runner. Requires Batmobile + Alfred + Looteer v2. |
| **Batmobile-1.0.12** | (shared library) | Pathfinder/explorer used by ArkhamAsylum and WonderCity. Does nothing on its own. |
| **HelltideRevamped-0.4** | Helltides | Maiden routes, chests, ore/herb, shrines, goblins, chaos rifts. |
| **HordeDev-1.3.9** | Infernal Hordes | Spires/masses, pylons, aether, boss room, talisman / GA / materials chest rewards. |
| **Reaper-main** (v1.7, Moogazoog) | Boss Runs | Native-teleport boss farmer for Andariel / Duriel / Varshan / Grigoire / Zir / Beast in Ice / Harbinger / Urivar / Butcher / Belial. Consumes Lair Keys / Greater Lair Keys / Betrayer's Husks (per-boss `key_tier`). Path-file walks primary, Batmobile auto-fallback. Belial chest dialog has live in-GUI alignment with center-aware ultrawide scaling, plus Astaroth / Bartuc as additional reward targets. |
| **WonderCity-main** | Kurast Undercity | Tribute selection, enticements, beacons, boss, final chest. Optional objective-first custom explorer (opt-in). |

## Required companion plugins (not bundled)

These are external prerequisites — install them separately into your scripts directory:

- **Alfred the Butler** — town services (stash / salvage / sell / repair / restock between activities). Used by every activity plugin in the suite, and by WarPigs for the via-Temis cleanup detour between WarPlans. Install separately into your scripts directory.
- **Looteer v2** — loot pickup. Required by Arkham and WonderCity; recommended for everything else.
- **Orbwalker** — combat. Each activity plugin has a "Manage orbwalker" toggle (default OFF) that lets WarPig drive `set_clear_toggle` / `set_block_movement` for you; if you'd rather drive orbwalker yourself, leave it off and configure orbwalker manually (typically Clear toggled on + Block Orbwalker Movement enabled, hard requirement for Infernal Hordes).

## Quickstart

1. Copy the eight plugin folders listed above into your scripts directory (alongside `Alfred`, `Looteer`, `Orbwalker`, etc.). Keep your existing settings and custom files. `audit/` contains reports/tests and is not a plugin.
2. Launch the game and load the framework.
3. Open each sub-plugin's menu once and configure to taste (loot modes, exit timers, glyph upgrade thresholds, tribute order, Reaper boss selection + Belial chest alignment, WarPug reroll click capture, etc.). Sub-plugin docs are in each subfolder's `README.md`.
4. *(Optional)* Open `Z | War Pug | War Plan Creator`, capture the **Reroll** and **Confirm** click positions for your war plan vendor (hover the in-game button, press the bound keybind), then tick **Enable**. WarPug can pick a path in Temis when the quest snapshot is valid and empty, WarPigs/Alfred have released control, and the creator has no held error.
5. Open `Z | War Pigs | Orchestrator` and tick **Enable**. With **Use teleport** on, WarPigs will run each WarPlan, detour through Temis for Alfred between activities, and turn in rewards automatically.
6. Start your combat / orbwalker script.

WarPigs takes over from there: it enables the matching activity, waits for its post-quest cleanup, then releases it before starting the next task. An empty quest log does not cancel unfinished rewards or dungeon transitions. Master disable requests a stop from every owned activity and retries failed stops.

## Boss Lair plan → boss mapping (Reaper)

WarPigs maps each boss-lair War Plan quest to a Reaper boss id:

| Quest substring | Boss |
|---|---|
| `WarPlans_QST_BossLair_Andariel` | andariel |
| `WarPlans_QST_BossLair_Harby` | harbinger |
| `WarPlans_QST_BossLair_Duriel` | duriel |
| `WarPlans_QST_BossLair_Varshan` | varshan |
| `WarPlans_QST_BossLair_PenitentKnight` | grigoire |
| `WarPlans_QST_BossLair_Zir` | zir |
| `WarPlans_QST_BossLair_MegaDemon` / `_Beast` / `_BeastInIce` | beast |
| `WarPlans_QST_BossLair_Urivar` | urivar |
| `WarPlans_QST_BossLair_Butcher` | butcher |
| `WarPlans_QST_BossLair_Belial` | belial |

A few quest suffixes are best-guesses (anything not marked CONFIRMED in `WarPigs-1.0.0/core/orchestrator.lua`). If a boss plan never triggers, enable **Log ALL quests** in the WarPigs menu, run that plan once to capture the real quest name, and update the map in `core/orchestrator.lua`.

## GUI options (WarPigs)

- **Enable** — master toggle. Off = no quest watching, all managed plugins forced off. Defaults to off on first load (don't auto-activate before you've configured anything).
- **Use keybind** — show the keybind row below. Off = the master toggle is the only gate.
- **Toggle Keybind** — when **Use keybind** is on, this hotkey gates WarPigs on top of the master toggle (key off / not toggled = same as Enable being off, releases all managed plugins). Mirrors HordeDev's keybind pattern.
- **Use teleport** — flip on to enable the via-Temis-Alfred preamble between every WarPlan. After the previous activity wraps up, WarPigs teleports the player to Temis, triggers Alfred for stash / salvage / repair, waits for him to finish (with a 3 s post-Alfred settle so a re-armed mini-cycle doesn't slip through), then calls `warplan.teleport_to_activity()` to drop you on the next quest. Costs an extra teleport per non-Pit handoff but guarantees clean inventory each cycle. Off = WarPigs lets each activity plugin handle its own travel.
- **Run pit after turn-in** — once a turn-in cycle completes, fill idle time with ArkhamAsylum when no plan creator is enabled. Enabled WarPug takes priority so it can create the next plan without waiting on filler. Real plans also preempt filler; an existing Pit run wraps up through its normal in-town cleanup gate.
- **Verbose logs** — print quest match/unmatch diffs to console.
- **Log ALL quests** — print every newly-seen quest name + id to console. Use this when adding a new activity or fixing a wrong boss-lair guess.

## Troubleshooting

- **Plugin not loaded** warning in the WarPigs menu → that plugin global wasn't published. Check load order, missing files, or a script error in that plugin.
- **Plan starts but plugin never activates** → flip on **Log ALL quests**, run the plan, and confirm the actual quest name contains the substring in the orchestrator map.
- **Plugin won't disable when plan ends** → the outgoing activity must finish its cleanup gate before the next task or teleport starts. Horde chest/reset/entry transactions remain protected. Defer thresholds now produce diagnostics; they do not fabricate completion or force the next activity. Inspect the outgoing plugin status if it never completes, or stop it explicitly.
- **Two plugins start at the same time** → WarPigs sequences managed handoffs and holds on failed disables. Avoid independently enabling another activity controller while WarPigs owns the cycle. External plugins and stale host status remain outside its control.
- **Bot stuck "settling Alfred"** → WarPigs holds for at least 6 s after firing Alfred's trigger so the status task has time to pick it up. After that it watches Alfred's `external_trigger` flip back to false (= picked up + done). 8 s pickup timeout if Alfred never went busy, 180 s absolute cap, then a 3 s post-Alfred settle re-checks for a re-armed mini-cycle before firing the warplan teleport.

## Changelog

### 2026-05-08
- **Reaper v1.1 → v1.7 (Moogazoog)**. Major rewrite. Inventory model swapped from "Materials + Sigils" run-types to a per-tier key system: Lair Keys drive Varshan / Grigoire / Zir / Beast / Urivar; Greater Lair Keys drive Duriel / Andariel / Butcher / Harbinger; Belial costs 2× Betrayer's Husk per run. Per-boss `key_tier` field in `data/enums.lua`. Native teleport via `teleport_to_boss_dungeon(sno)` — D4Assistant integration, `command.txt`, the entire map-click navigation system, and Boss Icon Alignment all removed. Three rotation modes (Manual single-boss, Round Robin, Random). Belial chest dialog has live in-GUI slider alignment with center-aware ultrawide scaling (`screen_w/2 + (ref_x − 960) × screen_h/1080`) and a crosshair overlay; Astaroth and Bartuc added as additional reward targets. Path-file walks remain primary; Batmobile auto-engages as fallback when path files fail or aren't present, plus a "Use Batmobile Navigation" toggle to force Batmobile primary.
- **WarPigs: Alfred handoff hardening**. New `POST_ALFRED_SETTLE` state (3 s) between `TEMIS_ALFRED` and `TELEPORTING` — re-checks `alfred_idle()` and bounces back to `TEMIS_ALFRED` if Alfred re-armed a mini-cycle (e.g. inventory rescan re-detected work). `ALFRED_MIN_DWELL = 6 s` minimum hold before exiting `TEMIS_ALFRED` regardless of reported state, covering the slow `main_pulse` pickup window where `alfred_idle()` flickers true between trigger queue and Alfred's status task running. `alfred_idle()` now also blocks on `s.external_trigger`, `s.need_trigger`, `s.inventory_full`, `s.need_repair`. Tracks `alfred_picked_up` via the `external_trigger=false` edge so we distinguish "Alfred didn't pick up yet" from "Alfred picked up and had nothing to do".
- **Batmobile: wall-path avoidance toggle**. New "Wall path" checkbox + radius slider (default 4 u). Partial paths whose endpoint lands within the radius of an unwalkable cell are skipped (16-angle sample scan), so the explorer picks a different frontier instead of wedging the bot against the obstacle. Explorer-target only — custom targets unaffected.
- **Suite-wide: `tasks/alfred.lua` standardisation**. ArkhamAsylum / HelltideRevamped / Reaper / WonderCity all updated to the same Alfred trigger pattern.

### 2026-05-07
- **WarPigs: via-Temis-Alfred preamble (huge)**. Every warplan teleport now detours through Temis and triggers Alfred first. New state machine: `IDLE → TO_TEMIS → TEMIS_ALFRED → POST_ALFRED_SETTLE → TELEPORTING`. Costs an extra teleport for non-Pit handoffs but guarantees a clean inventory between every WarPlan — no more starting Hordes at 30/33 inventory or showing up to a boss with yesterday's vendor trash. Off when **Use teleport** is off; falls through to the host's `teleport_to_waypoint` if available, otherwise straight to the warplan teleport / skip decision.
- **WarPigs: helltide combat hold**. New `enemies_near_player()` predicate (12 u). When holding the helltide buff AND a non-untargetable enemy is in melee range, the warplan teleport is deferred — incoming damage was interrupting the channel and looping the handoff. Town-to-town handoffs unaffected.
- **WarPigs: Hordes hold for chests, hard**. `chests_done()` is now a hard prerequisite for InfernalHordes' `disable_when`. The 60 s out-of-BSK timer only starts after chests finish. Per-entry `max_disable_defer_seconds = 300` overrides the global 120 s cap so the slow talisman + GA + materials chest cycle can't be force-disabled by the safety net. New `InfernalHordesPlugin.chests_done()` external API surfacing `tracker.finished_chest_looting`.
- **WarPigs: Undercity mid-run arrived_when**. `arrived_when` for `WarPlans_QST_Undercity` now also matches when the player is inside any `X1_Undercity_*` zone, so mid-run snapshots stop the orchestrator looping on "teleport retry — world/zone unchanged" while WonderCity is doing the actual run.
- **WarPigs: Helltide arrived_when uses helltide buff**. `WarPlans_QST_Helltide_TorturedGifts` got an `arrived_when = has_helltide_buff` predicate. Cold-start or post-respawn re-arm inside an active helltide zone now skips `warplan.teleport_to_activity()` instead of looping on retries while HR tries to chest. The buff is ground truth — true regardless of which specific Helltide_* zone we landed in.
- **WarPigs: same-activity continuation**. When the just-disabled plugin re-matches within 30 s with the same quest pattern (back-to-back same-type WarPlans), the teleport is skipped and the plugin re-enables in place. Different patterns on the same plugin (Andariel → Varshan) still teleport normally and Reaper retargets the new boss.
- **WarPigs: arrived_when + task-only handoff helpers**. New `start_warplan_teleport()` shared helper so the IDLE-ready and `TEMIS_ALFRED`-exit paths share the task-only / arrived_when / warplan-call decision.
- **WonderCity: explicit chest interact loop in `goto_chest`**. After landing on the final chest, the bot now interacts on a 1 s debounce with an 8 s timeout and disables orbwalker movement during the channel so it doesn't get yanked off mid-open. New INTERACTING / WAITING statuses; arrival distance tightened 3.0 → 2.0 u.
- **HelltideRevamped: chest cinder-decrement open detection**. Snapshots cinders at interact; if the count drops on a later tick the chest opened (game charged us) even when `:is_interactable()` lags by a frame. Stops the bot from re-firing interact on a chest that already gave loot. Interact cooldown extended past `CHEST_POST_OPEN_PAUSE` so an interrupted channel gets retried after the hold instead of being treated as success.
- **HelltideRevamped: `mark_chest_opened` and the post-open hold tick now stop Batmobile's `long_path` navigation**. `main_pulse` was driving `navigator.update + move` autonomously while `long_path.navigating` was true, repathing through the pause and pulling the player off the loot pile. Pause + clear_target alone wasn't enough — long_path is its own state.
- **HelltideRevamped: patrol unreachable-waypoint skip**. Two early signals — `BatmobilePlugin.set_target` returning false (waypoint inside the failed_target cooldown zone) or repeated `plen<=2 limit_partial` pathfinds against the same waypoint — advance `ni` by `WAYPOINT_LOOKAHEAD` instead of waiting for the 5 s stuck timer. After 3 consecutive unreachable waypoints, drop into `FREE_EXPLORE` so Batmobile's frontier explorer takes over until we make it out of the dead cluster. Counters reset on real progress (arrival or dead-cluster exit).
- **HelltideRevamped: Hawe_ZakFort excluded zone**. `data/zone_overrides.lua` adds an `excluded_zones` list. `helltide_task` refuses to run inside Hawe_ZakFort and `search_helltide` owns the teleport-away.
- **HelltideRevamped: chest stuck-detector + micro-partial improvements**. Stuck-detect range bumped 35 → 60 u (chests behind ledges were oscillating outside the old window). New micro-partial detector blacklists chests after 20 consecutive `plen<=2 limit_partial` pathfinds (geometry-blocked target — much faster than the 12 s no-progress fallback). Suppresses blacklisting at point-blank range so a monster interrupting the open animation can't kill a perfectly reachable chest.
- **HelltideRevamped: search-cycle cooldown**. After a full TP scan with no helltide found, wait 45 s before retrying. Was teleport-spamming through the entire helltide hour when no zone had spawned yet.
- **Batmobile: post-spell anchor**. After a movement-spell cast (dash / teleport), `last_pos` is overridden to the spell destination and a 0.3 s `pathfind_replan_cooldown` is applied. Host-side position update lags the cast by a tick or two — without this the next `find_path` ran from the pre-cast position and either backtracked or failed, causing visible stutter on dash-heavy builds.
- **WarPigs status line shows task state**. "WarPigs: watching quests" → "WarPigs: task TurnIn_Rewards [APPROACH_NPC]" so you can see what the bot is actually doing during task phases.
- **WarPigs turn-in escape hatch**. If the player is stuck outside town for 20 s (dungeon-exit plugin self-disabled, nobody navigating us out), the turn-in task self-teleports to Temis instead of blocking the orchestrator forever. Also gates IDLE→teleport on `alfred_idle()` to avoid cancelling Alfred's return portal mid-stash.

### 2026-05-06
- **Batmobile: path smoothing is now configurable**. New "Path smoothing step" slider (0–10, default 1.0) controls the LOS sample interval used when string-pulling A* paths. 0 disables smoothing entirely (raw grid path, safest near thin walls); 3–10 produces super-smooth paths with very few corners. Both `find_path` and `find_path_debug` use it.
- **WarPigs: teleport gate also waits on `pending_disable`**. Previously the orchestrator would happily fire `warplan.teleport_to_activity()` while the outgoing plugin's `disable_when` was still un-satisfied — so e.g. Hordes would still be wrapping up loot when we tried to teleport away. Gate now blocks until pending disables clear, and an in-flight teleport aborts if a pending disable appears mid-sequence. Task-only handoffs (TurnIn_Rewards manages its own teleports) skip the warplan call entirely.
- **Reaper: `kill_monsters` always runs while altar is active**. `shouldExecute` was bailing when no enemy was currently visible — but in the boss arena the bot would idle through fade-in / wave gaps. Now stays active for the whole altar-on window.
- **WonderCity: `kill_monster.shouldExecute` actually checks zone**. `utils.player_in_undercity` was being passed as a function value (always truthy) instead of being called — so the task ran outside the undercity. Added parens. Fixes phantom kill loops in town. Also added a one-shot per-run actor dump in `portal.lua` for tuning boss-room detection.
- **Reaper: `kill_monsters.reset()` no longer unblocks movement**. Other Reaper tasks expected `block_movement` to stay on between phases; clearing it on reset broke their assumptions.

### 2026-05-05
- **ArkhamAsylum: progress-orb pickup during pit explore**. Pit explore now scans for `TWR_ProgressOrb` actors every tick and walks straight to any within range via `BatmobilePlugin.navigate_long_path`, instead of waiting for Batmobile's frontier exploration to drift past one organically. Repaths every 2 s if Batmobile drops the long path; stops when within 5 units of the orb so the existing interactable-pickup logic takes over.
- **Reaper: orbwalker actually on during boss combat**. `kill_monsters.Execute()` now sets `orbwalker.set_clear_toggle(true)` + `set_block_movement(true)` at the top of every tick (and a new `task.reset()` clears both). Previously relied on whatever the previous task left behind — which after the altar interact was "off". `kill_monsters` was therefore running in a no-cast state until something else flipped orbwalker back on.
- **WarPug: reroll confirmed working**. `TABLE_ACTOR_NAME` set to `Warplans_Vendor` (was empty), and the reroll flow now goes APPROACH_TABLE → interact → WAIT_READY → REROLL_CLICK1 → CLICK2 — previously fired the reroll clicks without re-opening the panel. Tested through two consecutive reroll cycles. **Caveat**: WarPug only excludes `Warplans_NightmareDungeons`; if every reroll produces a tree where the only legal path passes through NMD nodes, the planner gives up after `MAX_REROLLS = 10` and you'll need to confirm a war plan manually. Also: 6 resolution presets now available in the GUI combo (1080p / 1440p / 1440UW / 4K / 5120UW / 8K, auto-picks the closest match to your screen) and the click-position crosshairs track sliders in real-time so calibration is finally pleasant.
- **WonderCity: NEW objective-first custom explorer (opt-in)**. New "Use custom explorer" toggle and a 434-line `custom_explorer.lua` task that replaces Batmobile's full-coverage sweep with a targeted BFS — wide-scans for enticements / Grand Beacon / exit warp pad and steers toward them in priority order, falling back to a radial frontier expansion from spawn. Off by default (Batmobile coverage stays the default). Companion "Skip monsters" toggle disables orbwalker during exploration and only re-enables for elites/champions/bosses so the bot doesn't dawdle on trash on the way to objectives.
- **WarPigs: pending-disable resolution edge case**. When a plugin's quest re-matched before its previous `disable_when` satisfied (e.g. a new Horde WarPlan appears while still in BSK), `pending_disable` could stay armed forever. Now once `disable_when` finally clears, the orchestrator forces the handoff — calls `plugin_disable` (which arms `teleport_pending` so `warplan.teleport_to_activity()` fires before the plugin re-enables next cycle).
- **Batmobile: explorer frontier iteration switched to `pairs()`**. The old `select_node_distance` / `select_node_direction` code walked `frontier_index` down from a monotonically-growing insert counter, so in long sessions it spent more time stepping over nil holes than scoring real frontiers. Now iterates `pairs(frontier_node)` directly and evicts visited entries during the same pass. Pure performance / memory cleanup.
- **NEW: WarPug — War Plan creator**. New plugin in the suite. When you're in Temis with no active `WarPlans_QST_*` quests, WarPug auto-selects and confirms a new war plan path through the host's `warplan` API. Nightmare Dungeon nodes (`Warplans_NightmareDungeons`) are always excluded. If no valid path exists, falls back to a configured reroll + confirm pixel click — same dual-resolution slider pattern as WarPigs (1920×1080 + 8K, picked by `utility.get_screen_width()`). Capped at 10 reroll attempts per cycle.
- **WarPigs: teleport sequence simplified to a single API call**. The whole `WAIT_TAB → WAIT_CLICK → WAIT_SETTLE` state machine, post-settle "still in town?" retry verifier, click-position overlay, F5 capture-cursor keybind, and dual-resolution click sliders are **gone**. Replaced with one call to `warplan.teleport_to_activity()` and a 5 s settle. Same gating as before (incoming activity visible for 2.5 s, Alfred idle), just no more pixel-click voodoo. The "Use teleport" toggle stays — flip it on and the orchestrator handles it.
- **ArkhamAsylum: glyphstone orbwalker suppression is now opt-in**. New "Disable orbwalker at glyphstone" toggle (default off). Yesterday's auto-suppression was firing for users who actually wanted skills active during the upgrade.
- **WonderCity: enticement_timeout setting now actually applies**. The slider value was being read from the GUI but never copied into runtime settings.
- **ArkhamAsylum: stop nuking the glyphstone**. `upgrade_glyph` now flips `orbwalker.set_clear_toggle(false)` whenever the boss is dead and the player is within 5 m of the glyphstone, so skill casts don't trigger while the bot is interacting. `exit_pit` flips it back on before firing the teleport channel.
- **Batmobile: lock out traversals during a custom-target final approach**. When a custom target (e.g. a portal on a ledge) had been returning partial paths and then suddenly resolves to a full path, the player has clearly just landed on the same surface as the target. New `all_trav_blocked_until` (60 s) suppresses every traversal selection for the duration of the approach, so nothing yanks the bot back across a ledge it just crossed.

### 2026-05-04
- **ArkhamAsylum: portal death-recovery routing**. The biggest pit-stall fix to date. `PORTAL_DETECTION_RADIUS` (25 m) keeps `portal` from engaging during exploration, but when the player dies near a portal and respawns at the floor checkpoint (often hundreds of m away) the portal goes invisible AND there are no frontiers in the area (already explored), so `explore_pit` just sat there. Now `get_portal()` snapshots the last interactable portal it saw (TTL 60 s); a respawn — detected as a >50 m position jump within the same world without `portal_just_used` — long-paths the player back toward that snapshot until the portal re-enters detection range. 45 s recovery timeout, 3 s repath retry.
- **Batmobile: "Full path only" explore mode**. New toggle in the Batmobile settings tree (off by default). When on, any explorer frontier whose A* result comes back partial — regardless of length — is skipped immediately and marked failed so the explorer picks the next candidate. Stops the bot from confidently marching toward unreachable cells across cliffs / floor seams. Comes with an `Explore path budget (ms)` slider (50–500 ms, default 150 ms) so winding routes have enough A* time to actually resolve. The pathfinder also now treats per-call time-caps as a floor as well as a ceiling, and raises `iter_limit` proportionally so iter-cap doesn't fire before the time budget is consumed.
- **WarPigs: dual-resolution click sliders**. Replaced the single 0–3840 / 0–2160 slider pair (and the unreliable F5 capture-cursor that depended on a `slider_int:set()` method this host doesn't expose) with two explicit pairs: **1920×1080** and **8K (7680×4320)**. At runtime the orchestrator reads `utility.get_screen_width()` and picks the matching pair (≥3840 → 8K). Cleaner, no more invisible captures.
- **HelltideRevamped: Maiden boss farming**. New "Do Maiden" toggle in the HR settings tree. When on (and you've got Helltide Coin Hearts in inventory), the bot routes to the maiden altar for the current zone, inserts hearts at any interactable altar in range, and stays pinned to the lock zone for the boss fight + adds — combat happens *inside* the lock so we don't drift off chasing strays. Sub-option: "Disable Maiden at Cinders" — stop running maiden once you've banked N cinders so you can go spend them on chests. 7 zones supported (Frac_Tundra_S, Scos_Coast, Kehj_Oasis/HighDesert, Hawe_Verge/ZakFort, Step_South); altar positions verified against the archived `helltide_maiden_auto` plugin's data.
- **WarPigs: arm teleport on self-disable handoff**. `plugin_disable()` is the normal arm site for the Tab+click teleport, but when a plugin self-disables (e.g. Reaper finishing its rotation via its own `main.lua`) that path is bypassed. Now we also arm `teleport_pending` on the self-disable branch, so Reaper → next plugin doesn't enable in the boss room when Reaper's own teleport silently failed.
- **WarPigs: capture-cursor uses in-memory cache**. The `slider_int` widget on this host doesn't expose `:set()` (errors with `attempt to call method 'set' (a nil value)`), which broke the F5 capture flow — the slider value never updated. Switched to a module-level cache that overrides the slider value once captured. Manual slider edits still work on a fresh load; capture wins after that until script reload.
- **Batmobile: trap-escape blocks non-directional ping-pong partners**. Non-directional traversals (HandOverHand, FreeClimb without Up/Down) at the same z form ping-pong pairs — after crossing one, the partner sits on the other side waiting for the player to drift back into trap state. The escape logic only blocked opposite-direction gizmos, so non-directional pairs were never blocked and the bot bounced 6+ times. Now same-name same-z (within 1 m) non-directional partners get the same trap-blacklist treatment so the bot commits to its chosen crossing.
- **ArkhamAsylum: Choron's Soul orb sweep**. After the soul is exhausted, the bot now walks a 6-point hex pattern (radius 4.5 m, 12 s budget) around the soul's last-known position to vacuum up XP orbs that landed beyond auto-pickup range. Driven off a snapshotted position so it still works after the actor despawns. Skipped entirely if no clicks fired.
- **Batmobile: reject very-short partial paths**. A* returning only 1-3 steps toward a target >8 m away means there's a wall / cliff / gap in the way and the partial tail wedges the player into it. Now rejected up-front so the failure handler fires faster (nudge / failed-direction recording / blacklist). Custom targets and traversal-routing keep partial paths regardless.
- **WarPigs: "Run pit after turn-in" filler**. New toggle. Once a `WarPlans_QST_TurnIn_Rewards` cycle completes (matched → unmatched edge), any subsequent dead air with no active WarPlans quest is filled by enabling `ArkhamAsylumPlugin`. Real WarPlans always preempt the filler (it's injected only when `next(wants) == nil` AND no internal task is active); when a new WarPlan arrives, the normal disable flow pulls Arkham out via its `in_town_disable_when` so the pit run finishes cleanly. The Tab+click teleport sequence is also suppressed for the filler since Arkham handles its own TP-to-town-then-walk-to-pit-tower.
- **WarPigs: post-settle "still in town?" retry**. After `WAIT_SETTLE`, plugins listed in `TELEPORT_REQUIRE_LEAVE_TOWN` (currently just `InfernalHordesPlugin`) are verified to actually be out of town — if not, the click missed (UI not ready, wrong coords) and the orchestrator restarts Tab+click up to 4 retries before giving up. Stops Hordes from being enabled in Temis where it crashes on launch.
- **HelltideRevamped: zone overrides**. New `data/zone_overrides.lua`. When WarPigs (or any external trigger) drops the bot into a non-canonical Helltide zone (first entry: `Sanctuary_Eastern_Continent / Skov_Celestia`), `helltide_task` takes ownership during the helltide hour even before the buff lands and walks to a configured entry vec3 so the buff applies, then falls through to the no-waypoint free-explore fallback. `search_helltide` is suppressed inside override zones so it doesn't teleport back to a known town and undo the WarPigs TP.
- **ArkhamAsylum: kill_boss remembered-hunt suppression**. When the player dies and respawns at the pit entrance with a cached `boss_position` deep on another floor, `navigate_long_path` keeps returning false and the task spun forever pausing Batmobile with no target. After 3 consecutive `long_path` failures OR 25 s of no distance improvement, the remembered hunt is suppressed for 60 s — yields to `portal` / `explore_pit` / `kill_monster` so the bot moves; if the boss is still alive on this floor the 60-unit `find_boss()` scan picks it up as we move and clears the cooldown immediately.
- **ArkhamAsylum: Choron's Soul rewrite (still UNTESTED end-to-end but more robust)**. Replaces the old "click → blacklist if stuck" with a real sequencer: walk → 4 s loot pickup window → click → 4.5 s charge → click → … up to 10 clicks per soul (~45 s) or until the actor self-deactivates. Walk timeout 30 s blacklists souls that are genuinely unreachable. `consume_chorons_soul` was *also* moved **above** `upgrade_glyph` in the task manager — both consume the same glyph upgrade chances and the user picked "burn for XP" — and `upgrade_glyph.shouldExecute()` got a defensive `soul_blocking()` gate as belt-and-suspenders.
- **HordeDev: WarPlans Talisman Chest**. New "Always Open WarPlans Talisman Chest" toggle (default off). When on, opens the `Warplans_BSK_TalismanChest` actor *first*, before GA / selected. Chest-picker rewritten around a static `chest_order = {TALISMAN, GREATER_AFFIX, SELECTED}` with per-entry `chest_enabled()` predicate + `next_enabled_index()` skipper, so disabled chests are skipped cleanly instead of wasting ticks on closed actors.
- **WarPigs: death recovery + incoming-settle for teleport handoff**. The orchestrator now detects `is_dead()` first thing each tick: aborts any in-flight transition, re-arms `teleport_pending` so the Tab+click reruns after respawn, and calls `revive_at_checkpoint()` until it takes. New `TELEPORT_INCOMING_SETTLE = 2.5 s` waits for the next WarPlan's quest entry to actually render in the panel before we press Tab — fixes the Helltide → Turn-in handoff where Tab fired before the Turn-in quest existed and we'd click stale (empty) UI. The `in_town_attribute` precondition was *removed* (HR doesn't auto-TP to town on disable, so it deadlocked the handoff); the click itself does the traveling.
- **WarPigs: Alfred idle predicate refined**. Switched from `all_task_done` to `s.paused || !s.trigger_tasks`. `all_task_done` is false on cold start until Alfred runs a full cycle — on a fresh launch with nothing for Alfred to do that gated the teleport forever. New predicate also passes when Alfred is `paused` by another plugin (e.g. HR pauses Alfred while running and never resumes on disable).
- **Batmobile: failed-direction memory (explorer + navigator)**. Whenever the navigator abandons a target (partial-path no-progress or N consecutive A* failures), it records a unit vector player→target with origin and timestamp (TTL 25 s, max 12 entries, near-duplicates coalesced). The explorer's frontier scoring now subtracts a direction-aligned penalty (45° cone, distance + age weighted) from candidates pointing into recent-failed bearings, biasing selection away from walls / unreachable clusters instead of repeatedly picking the same dead frontier. Partial-path no-progress threshold tightened 4 s → 2.5 s (give traversals a chance, don't sit stuck), and the custom-target unreachable radius bumped 15 → 25 so HR patrol skips the whole blocked cluster on re-set.
- **HelltideRevamped: no-waypoint zone fallback**. When WarPigs (or any external trigger) drops the bot into a Helltide zone whose region prefix isn't in `enums.helltide_tps` (e.g. `Skov_Celestia`), the state machine used to ping-pong INIT ↔ EXPLORE_HELLTIDE forever because `tracker.waypoints` stayed empty. Now `check_and_load_waypoints()` returns false on miss and the task hands movement off to Batmobile's free-explore for that session — `check_events` still drives chests / ore / herbs / shrines / goblins / pyres via actor scans, only the wander-between-events movement source changes.
- **ArkhamAsylum: Choron's Soul consumer (UNTESTED)**. New "Use Choron's Soul" toggle (default off). After the pit boss dies, walks to the `Warplans_Pit_ChoronsSoul` interactable that spawns next to the Awakened Glyphstone and burns remaining glyph-upgrade chances into experience. Sits below `upgrade_glyph` and above `alfred / portal / exit_pit` in the task list so glyphs upgrade first, then the soul converts the leftover. **Not yet verified end-to-end — flag at your own risk; please report behaviour.**
- **WarPigs: teleport transition — loot-safe trigger + reliability fixes**. The teleport sequence now fires from `plugin_disable()` (after the previous activity's `disable_when` predicate satisfies — Reaper kill+60 s, Pit/Undercity arrival in town, etc.) instead of on the next WarPlan's pattern-match edge. WarPlan quests unmatch the moment the boss dies, so the old trigger was Tab+clicking out of the dungeon before chests were opened or Alfred had salvaged. Now the orchestrator waits for chest loot, Alfred to finish, and `PLAYER_IN_TOWN_LEVEL_AREA` to flip true before tabbing out. Cold-start (first activity of a session) is handled explicitly. Click delay bumped 1.0 s → 1.5 s (the Quest panel fade-in was outracing the click on slower frames), and an explicit mouse-move now precedes the click so panels that gate clicks on a fresh mouse-position event actually register it. Holding-state logs explain *why* the sequence is paused (`in_town=… alfred_idle=…`).
- **WarPigs: teleport transition (experimental)**. New "Use teleport" toggle in the WarPigs menu. Between activities, the orchestrator can press **Tab** (open map / quest list), click a configured pixel target (typically the next WarPlan's quest icon), then wait for the teleport channel to settle before enabling the next plugin. Sub-options: X/Y pixel sliders, **F5 capture-cursor** keybind (hover the in-game target, press F5 to set X/Y), and a "Show click position" overlay that draws a green crosshair at the target plus a fading yellow ring on every scripted click. The enable gate and any task ticks (e.g. turn-in) are held until the sequence settles so nothing competes with the click.
- **HordeDev: pylon priority rebalanced**. `data/pylons.lua` reordered for the current season — Thriving / Fiendish Masses, Fiendish Legions, Blistering Hordes, and Surging Elites pushed up; Spire-focused boons demoted; a few stale entries removed.
- **Suite-wide: teleport & interact channel debouncing**. `teleport_to_waypoint`, `reset_all_dungeons`, and `interact_object` are multi-second channels — re-firing them every tick *cancels* the previous channel, so the bot was running around in place instead of arriving / interacting. Added per-task debounces in `WarPigs/turn_in_rewards` (6 s), `HelltideRevamped/search_helltide` idle-town TP (6 s), `HordeDev/exit_horde` Teleport mode (5 s), `WonderCity/exit_undercity` (always-on, was party-only — `confirm_delay` covers it), `ArkhamAsylum/exit_pit` (always-on, was party-only), and `WonderCity/interact_enticement` for beacons (1 s; the timeout clock now also starts on range entry instead of `is_interactable() == false` so a stale-flagged Grand Beacon can't lock the bot).
- **Suite-wide: stop Batmobile `long_path` before exit-task pauses**. Batmobile's main pulse re-runs `navigator.unpause() + update() + move()` every frame while `long_path.navigating` is true, which overrode our `pause` and walked the player toward the previous target — that movement cancelled the exit teleport channel. `ArkhamAsylum/exit_pit`, `WonderCity/exit_undercity`, and `HordeDev/exit_horde` (Teleport mode) now `stop_long_path()` and `clear_target()` before pausing.
- **WarPigs: activity-plugin preemption priority**. When multiple WarPlan quests match simultaneously, only the highest-priority one stays active. Static priorities: Pit (100) > Hordes (90) > Boss runs (80) > Undercity (50) > Helltide (40). Fixes the case where finishing a Kurast boss with an active Pit WarPlan would loop back into another Undercity run instead of handing off to Arkham. Demoted plugins fall through the normal disable path so an in-flight activity still gets to wrap up.
- **WarPigs: turn-in-rewards waits for town**. The IDLE→teleport branch now reads `PLAYER_IN_TOWN_LEVEL_AREA` and stays put while a dungeon plugin is still mid-exit. Stops the case where the dungeon's exit teleport and the reward turn-in's teleport fired together and cancelled each other's channels.
- **ArkhamAsylum + WonderCity: short-range `force_move_raw` near the pit obelisk / spirit brazier**. Both targets sit on non-walkable terrain, so Batmobile's A* returns `limit_partial`, the partial-path watchdog clears the custom target, and the explorer drags the bot off to a frontier ~17 units away — cue ping-pong: walk to objective → "no target, selecting new" → walk to frontier → re-fire → loop. Inside 7 m the navigator now skips Batmobile entirely and uses `pathfinder.force_move_raw`, so the bot holds position long enough to interact.
- **ArkhamAsylum: max glyph level slider 100 → 150**. Future-proofing for season patches that raise the cap.
- **Reaper: configurable home town**. New "Home town" combo box (Temis / Cerrigar) in the Reaper settings tab. All hard-coded Cerrigar references in `main.lua`, `interact_altar.lua`, `navigate_to_boss.lua`, and `sigil_complete.lua` now resolve through `settings.town_zone` / `settings.town_waypoint`. Defaults to **Temis** to match ArkhamAsylum and Alfred — set this to whatever your other plugins use so all the between-run TPs land in the same place.
- **WarPigs: re-fire enable on pattern change**. ReaperPlugin can be driven by multiple WarPlan quests (e.g. `_Zir` → `_Varshan`). Previously, when the matched pattern changed for an already-owned plugin the orchestrator's edge-trigger short-circuited and the plugin kept running the old boss. Now the orchestrator tracks `last_enabled_reason` per plugin; on a pattern change it re-fires `enable()` so the plugin's hook (e.g. `run_boss('varshan')`) takes effect, and clears the stale defer (the old boss's kill+60 s timer is irrelevant once we hand off).
- **WarPigs: keybind toggle + safer default**. New `Use keybind` checkbox + `Toggle Keybind` row gates WarPigs on/off via a hotkey on top of the master toggle (mirrors HordeDev's pattern). When the keybind is unset/untoggled WarPigs acts as if disabled — managed plugins are released and run autonomously again. The master `Enable` checkbox now defaults to **off** so a fresh load doesn't immediately seize control of every plugin in the suite before you've configured it.
- **WarPigs: Zir quest name confirmed**. `WarPlans_QST_BossLair_Zir` is the real quest name (id=2317384, captured 2026-05-03). Replaces the `_S2VampireLord` asset-name guess in the orchestrator map and README boss-lair table.
- **ArkhamAsylum: dedicated boss task + glyphstone anchor**. Pit guardian now has its own `kill_boss` task (higher priority than portal / explore_pit / kill_monster) so trash can't pull the bot off the boss. Boss position is remembered across player death — after revive the bot pathfinds back without exploring or chasing trash. After the kill, an anchor is set on the death position (then snapped onto the glyphstone when it spawns) and `explore_pit` holds within 6 m of it indefinitely instead of relying on the old 10 s fixed freeze, so a slow-spawning glyphstone can't cause a wander-off.
- **HordeDev: Exit mode toggle (Reset / Teleport)**. New GUI option. Teleport mode skips the boss-room walk and the 5 s wait — straight TP to the Library waypoint after the run.
- **HordeDev: graded stuck recovery in `walking_to_horde`**. Replaces the single 12 s "re-teleport to Library" watchdog with three escalating stages: 4 s nudge (re-snap to nearest waypoint, force fresh Batmobile target with smaller lookahead) → 8 s micro (bypass Batmobile, drive per-waypoint with the explorer fallback) → 12 s tele (re-teleport to Library, existing behavior). 20 s recovery cooldown so it can't thrash.
- **Reaper: Doom/theme chest removed**. D4's 2026-05-03 patch removed the seasonal `S12_Prop_Theme_Chest_*` actor entirely. The whole THEME phase, doom-chest scanning, `sigil_chest_done` state, and 20 s post-chest hunts are gone (~190 lines from `open_chest.lua`, ~30 from `sigil_complete.lua`). Material runs now complete on the EGB/boss chest open; sigil runs complete on the enemy-cleared timer alone.
- **WonderCity: Exit mode toggle (Reset / Teleport)**. Same option as HordeDev — Teleport mode tps back to the configured town waypoint instead of resetting all dungeons.
- **WonderCity: brazier/portal actor lookup fix**. `get_spirit_brazier()` and `get_entrance_portal()` switched from `get_ally_actors()` (which returned nil — the brazier and entrance portal are not in the ally list) to `get_all_actors()`. Previously the enter task would stick on idle waiting for an actor it could never see.
- **WonderCity: brazier interact watchdog**. If `interact_object` is spammed at the brazier for 3 s with no vendor screen response, lower the close-enough threshold (2.0 m → floor 0.6 m, 0.4 m steps) so the next tick walks closer instead of looping forever at "almost there." Resets on success.
- **WonderCity: `walk_kurast` yield-window fix**. `shouldExecute` now yields at the same distance `Execute` considers "arrived" — closes a small window where the walk task returned true while doing nothing, blocking `enter_undercity` from walking the last few meters to the brazier.

### 2026-05-03
- **Reaper: external rotations are now strictly single-shot**. Fixes a bug seen in the wild where a stuck/non-despawning chest (butcher this season) let the altar re-arm up to 6× before WarPigs's 120 s force-disable kicked in. New `external_consumed` flag locks `consume_run` to one call per rotation; the altar task hard-rejects any further interaction once that fires; the material-inventory recheck (which would otherwise re-extend the run) is skipped entirely for external rotations.
- **Reaper: 25 s "no chest still counts" window for external runs**. Some war plans now complete on the kill alone — no chest drops at all. After 25 s post-altar with no chest, the rotation force-completes so WarPigs's 60 s post-kill timer can start cleanly and the plan moves on. Inventory-driven (non-orchestrator) runs keep the original 60 s deadlock-retry path.
- **WarPigs: extra Beast-in-Ice quest aliases**. Map now lists `WarPlans_QST_BossLair_MegaDemon` (asset-name guess), `_Beast`, and `_BeastInIce` — whichever name Blizzard actually used will match. Use the WarPigs **Log ALL quests** option to confirm and trim the misses.

### 2026-05-02
- **WarPigs orchestrator: transition sequencer**. Adds a 5 s settle gap between disabling the outgoing plugin and enabling the next one, plus a 120 s safety cap so a stuck activity can never block the orchestrator forever. Pit / Undercity share an in-town disable predicate (waits for `PLAYER_IN_TOWN_LEVEL_AREA`); Helltide cuts immediately when the quest ends. Boss runs use a Reaper kill tracker (snapshots `total_runs` at enable; defers disable for 60 s after the counter ticks).
- **WarPigs orchestrator: enable hardened**. Wraps plugin `enable()` in `pcall` and trusts `status()` over the exit path — a partial enable (e.g. HR setting `main_toggle` then crashing on a missing `keybind_toggle`) no longer infinite-loops re-enabling. Self-disabled plugins (e.g. Reaper "Nothing to farm — Stopping") short-circuit, clearing deferrals so the next plan can start immediately.
- **HelltideRevamped: missing GUI element guard**. `HelltideRevampedPlugin.enable()` / `disable()` no longer crash when `gui.elements.keybind_toggle` is absent — important for headless / orchestrator-driven enables.
- **HordeDev: walking-to-horde stuck watchdog**. If the player hasn't moved 1.5 m in 12 s while the `walking_to_horde` task is active (post-teleport cooldown elapsed), re-teleport to the Library waypoint and reset Batmobile state. 20 s recovery cooldown so it doesn't thrash.

## Credits

Each sub-plugin lists its own author/contributors in its folder's `README.md`. This suite is a curated bundle, not a single-author project — credit goes to the original plugin authors:

- Zewx · Pinguu · NotNeer · Letrico · SupraDad13 · Lanvi · RadicalDadical55 · Diobyte · TesXter · Moogazoog • and the HordeDev community thread

Bundled and orchestrated by **Zewx**.
